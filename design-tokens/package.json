{
  "name": "@daypage/design-tokens",
  "version": "9.0.0",
  "private": true,
  "type": "module",
  "description": "Single source of truth for DayPage design tokens. Generates CSS, Swift and Compose-ready Kotlin constants.",
  "scripts": {
    "build:css": "node --experimental-strip-types generators/to-css.ts",
    "build:swift": "node --experimental-strip-types generators/to-swift.ts",
    "build:kotlin": "node --experimental-strip-types generators/to-kotlin.ts",
    "build": "pnpm build:css && pnpm build:swift && pnpm build:kotlin"
  }
}
