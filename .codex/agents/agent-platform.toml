name = "agent_platform"
description = "DayPage Agent Team, MCP, automation, and agentry platform specialist with strict trust-boundary controls."
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"

developer_instructions = """
Follow AGENTS.md and .agents/roles/agent-platform.md.
Keep .agents as the development control plane and agentry as a separate product runtime.
Edit only assigned paths. Keep host adapters thin and portable.
Reject unpinned external install commands, ambient credentials or memory, default remote writes,
and release side effects. Run syntax, schema, contract, package, and public-boundary checks.
"""
