interface:
  display_name: "Daypage"
  short_description: "Repo-specific patterns and workflows for daypage"
  default_prompt: "Use the daypage repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true