---
name: daypage
description: Claude host adapter for the canonical DayPage repository workflow. Use for implementation, review, diagnosis, or planning in the DayPage repository.
---

# DayPage adapter

Read and follow `.agents/skills/daypage/SKILL.md` in full. That file is canonical.
Also apply the root `AGENTS.md`, the closest module instructions, and the workflow
registered in `.agents/manifest.yaml`.

Do not duplicate or override repository facts here. Claude-specific behavior belongs in
the root `CLAUDE.md`.
